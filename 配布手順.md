# Voice Input Studio ― 配布・自動アップデート 全体手順

これは **あなた（配布する側）** 用の手順書です。
受け取る人向けの案内は `release\はじめにお読みください.txt`（ZIPに同梱）です。

---

## 全体像

```
  あなたのPC                    Dropbox                     相手のPC
 ┌──────────┐  ①アップロード ┌──────────────┐  ③ZIPを渡す ┌──────────┐
 │ release.ps1│ ───────────→ │ version.json   │            │ 解凍して  │
 │ でビルド   │              │ (更新情報)     │ ←②自動確認─ │ exe実行   │
 │            │ ───────────→ │ VoiceInput…exe │ ←  自動DL ── │           │
 └──────────┘              └──────────────┘            └──────────┘
```

- 配布形態：**ZIPを解凍して `VoiceInputStudio.exe` を実行**（インストール不要）
- 置き場所：**Dropbox の直リンク（`?dl=1`）**
- 更新方式：相手のアプリが起動時に `version.json` を見て、新しければ「更新しますか？」と通知
- APIキー：**相手が自分のキーを入力**（あなたに費用はかかりません）

---

## 【最初の1回だけ】セットアップ

自動更新の「見に行く先URL」をアプリに焼き込む必要があるため、初回だけ少し手順があります。

### 手順1：version.json を Dropbox に置いてリンクを取得
1. `release\version.json` を Dropbox の好きなフォルダにアップロード
   （例：Dropbox に `VoiceInputStudio` フォルダを作ってそこへ）
2. そのファイルの「共有リンクをコピー」
3. リンク末尾の `&dl=0` を **`&dl=1`** に書き換える
   - 例：`https://www.dropbox.com/scl/fi/xxxx/version.json?rlkey=yyyy&dl=1`

### 手順2：リンクをアプリに設定
- `voice_input\core\updater.py` を開き、`MANIFEST_URL = "..."` の中身を
  手順1で作ったリンクに置き換える。
  （※ この作業は Claude に「このリンクを設定して」と頼めば代行できます）

### 手順3：ビルド
```powershell
cd "G:\マイドライブ\Antigravity\01_System\voice_input_app"
scripts\release.ps1 -Version 1.5.0 -Notes "初回リリース"
```
→ `release\` に次の3つができます：
- `VoiceInputStudio-v1.5.0.zip` … 相手に渡す配布用（ガイド同梱）
- `VoiceInputStudio.exe` … 更新用の単体EXE
- `version.json` … 更新情報

### 手順4：EXE を Dropbox に置いてリンクを取得
1. `release\VoiceInputStudio.exe` を **version.json と同じDropboxフォルダにUP**
2. 共有リンクをコピーし、末尾を **`?dl=1`** にする
3. そのリンクを2か所に設定：
   - `release\version.json` の `"exe_url"` に貼る
   - `scripts\release.local.json` を新規作成し、次の内容を保存
     （これを置くと、次回以降このリンクが自動で埋まります）
     ```json
     { "exe_url": "ここにexeの?dl=1リンク" }
     ```
4. 書き換えた `version.json` を Dropbox に **上書きアップロード**

これでセットアップ完了です。

---

## 【毎回】相手に渡すとき

1. `release\VoiceInputStudio-v○.○.○.zip` を相手に送る
   （メール添付・ギガファイル便・Dropbox共有など、何でもOK）
2. 相手は解凍 →「はじめにお読みください.txt」に従ってキー入力 → 使い始める

---

## 【毎回】アプリを更新（新バージョンを配る）とき

アプリを直したら、この3ステップだけ：

1. ビルド（バージョン番号は前回より上げる）
   ```powershell
   scripts\release.ps1 -Version 1.5.1 -Notes "ここに変更点を書く"
   ```
2. できた `release\VoiceInputStudio.exe` を Dropbox に **上書きアップロード**
3. できた `release\version.json` を Dropbox に **上書きアップロード**

これだけで、**配布済みの全員のアプリ**が次回起動時に
「新しいバージョンがあります。更新しますか？」と表示し、自動で最新版になります。

> 📌 ポイント：Dropbox は「同じファイルを上書き」すれば共有リンクが変わりません。
> だから一度配ったZIPの中のアプリが、ずっと最新を追いかけられます。
> （ファイル名を変えて新規アップすると別リンクになるのでNG。必ず上書き）

---

## バージョン番号のルール

- `1.5.0` → `1.5.1`（小修正）→ `1.6.0`（機能追加）のように **必ず数字を上げる**
- 下げたり同じにすると「新版」と判定されず、更新が届きません
- 基準は `voice_input\__init__.py` の `__version__`（release.ps1 が自動で書き換えます）

---

## よくある質問・トラブル

**Q. 相手のセキュリティソフト/SmartScreenが警告する**
A. 署名なしEXEのため出ます。「詳細情報」→「実行」で起動可。ガイドにも記載済み。
   どうしても気になる場合はコード署名証明書の購入が必要（年1〜数万円）。

**Q. 自動更新が「フォルダに書き込めません」と出る**
A. 相手がアプリを Program Files など権限の要る場所に置いています。
   デスクトップ等へフォルダごと移動してもらえば解決します（ガイドにも注意書きあり）。

**Q. 更新が相手に届かない**
A. ①version.json を上書きアップロードしたか ②`version` を前より上げたか
   ③`exe_url` のリンクが `?dl=1` で正しいか、を確認。

**Q. 開発中（python -m voice_input）でテストすると更新が動かない**
A. 自動更新は配布版（.exe）でのみ動作します（仕様）。開発中は確認だけ行われます。

---

## 関連ファイル

| ファイル | 役割 |
|---|---|
| `voice_input\core\updater.py` | 自動更新の本体（`MANIFEST_URL` を設定する場所） |
| `scripts\release.ps1` | ビルド＋配布物作成（`-Version` 必須） |
| `scripts\release.local.json` | exeの固定リンクを覚えておく場所（初回に作成） |
| `release\version.json` | Dropboxに置く更新情報 |
| `release\はじめにお読みください.txt` | ZIP同梱の相手向けガイド |
