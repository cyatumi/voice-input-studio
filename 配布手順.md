# Voice Input Studio ― 配布手順

> **まずはフォルダ直下の「★ つかいかた・くばりかた.txt」を見てください。**
> 普段の操作はそれだけで足ります。これは技術的な補足メモです。

## いまの配布方式（現行・2026-06）

配布は **GitHub Actions による自動ビルド → GitHub Releases 公開** です。
（以前の Dropbox 手動アップロード方式は廃止しました。）

- **新バージョンの公開:** フォルダ直下の
  `★ 人に配る（新バージョンを公開）.bat` をダブルクリック → 画面の案内に従うだけ。
  （中身は `scripts\publish_easy.ps1` → `scripts\publish.ps1`。バージョン番号を付けて
  コミット＋タグを push すると、`.github/workflows/release.yml` が exe をビルドして
  Releases に公開します。所要 約8分。）

- **相手に渡すもの:** ファイルではなく **リンク1本**。常にこの固定URLを送る：
  - https://github.com/cyatumi/voice-input-studio/releases/latest
  - 相手はこのページの ZIP をDL → 解凍 → `VoiceInputStudio.exe` を実行。
  - 相手は自分のAPIキーを入力して使う（配布者に費用は発生しない）。

- **自動更新:** 配布済みアプリは起動時に
  `releases/latest/download/version.json` を見て、新しければ更新を促す。

## メモ
- 署名なし exe のため、初回は SmartScreen 警告が出る（「詳細情報」→「実行」）。
- 配布サイズは約94MB（v1.5.2で290MBから削減済み）。
